quarkus:
  http:
    port: 8081
  datasource:
    db-kind: postgresql
    username: postgres
"%dev":
  kafka:
    bootstrap:
      servers: kafka:9092
  mp:
    messaging:
      incoming:
        time-in:
          topic: time
          auto:
            offset:
              reset: latest
            group:
              id: ${quarkus.uuid}
  quarkus:
    datasource:
      password: example
      jdbc:
        url: jdbc:postgresql://localhost:5432/
    hibernate-orm:
      database:
        generation: drop-and-create
"%test":
  quarkus:
    kafka:
      devservices:
        enabled: false
